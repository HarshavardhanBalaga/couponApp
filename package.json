{
    "name": "coupon-app",
    "version": "1.0.0",
    "description": "Full Stack Coupon Distribution App",
    "scripts": {
      "start": "cd backend && npm install && npm start",
      "build": "cd frontend && npm install && npm run build",
      "heroku-postbuild": "npm run build"
    },
    "engines": {
      "node": "18.x"
    }
  }
  